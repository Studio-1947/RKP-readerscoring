"use client";

import { FormEvent, useEffect, useRef, useState } from "react";
import Image from "next/image";
import { BookOpen, Info, Languages, Mic, MicOff, Play, RotateCcw, Share2, Sparkles, Timer, Volume2 } from "lucide-react";
import { ReadingScore, scoreReading } from "@/lib/scoring";
import { saveReaderAttempt } from "@/lib/reader-storage";

type Language = "hi" | "en";
type Status = "ready" | "reading" | "details" | "result" | "unsupported";
type Passage = { id: string; sequence: number; title: string; difficulty_editorial: string; lines: string[]; reference_text: string; word_count_whitespace: number };
type Details = { name: string; age: string; phone: string; email: string; place: string; consent: boolean };
type Props = { passages: Passage[] };

const words = {
  hi: { title: "राजकमल रीडर", motto: "अपनी आवाज़ में साहित्य", new: "नई रचना", passage: "पाठ", listen: "पहले सुनें", stop: "सुनना रोकें", guide: "शांत जगह या headphones बेहतर हैं", ready: "तैयार होने पर शुरू करें", help: "अपनी आवाज़ में पंक्तियाँ पढ़ें", listening: "आपकी आवाज़ सुन रहे हैं", clear: "आराम से और स्पष्ट पढ़ें", start: "पढ़ना शुरू करें", finish: "पढ़ना पूरा करें", unsupported: "इस browser में Hindi voice recognition उपलब्ध नहीं है। Chrome या Edge पर खोलें।", profileTag: "स्कोर देखने से पहले", profileTitle: "अपनी जानकारी भरें", profileHelp: "अपना परिणाम देखने के लिए ये छोटे विवरण भरें।", name: "पूरा नाम", age: "आयु", phone: "फोन नंबर", email: "ईमेल (वैकल्पिक)", place: "शहर / स्थान", consent: "मैं सहमत हूँ कि राजकमल मेरे score के लिए ये विवरण इस्तेमाल कर सकता है।", privacy: "ये विवरण इस prototype में केवल आपके मौजूदा browser session में रहते हैं; अभी कहीं save नहीं होते।", view: "मेरा स्कोर देखें", result: "आपका परिणाम", great: "बहुत सुंदर पाठ!", score: "कुल स्कोर", accuracy: "शुद्धता", fluency: "प्रवाह", completion: "पूर्णता", speed: "गति", retry: "फिर पढ़ें", share: "परिणाम शेयर करें", missing: "यह जानकारी भरें।", phoneError: "मान्य फोन नंबर भरें।", ageError: "5 से 120 के बीच आयु भरें।", emailError: "मान्य ईमेल भरें।", voiceError: "Hindi voice उपलब्ध नहीं है। अपने device की voice settings जाँचें।", copied: "आपका परिणाम कॉपी हो गया है।" },
  en: { title: "Rajkamal Reader", motto: "Literature in your voice", new: "New passage", passage: "PASSAGE", listen: "Listen first", stop: "Stop listening", guide: "A quiet space or headphones work best", ready: "Ready when you are", help: "Read the passage aloud in your own voice", listening: "Listening to your voice", clear: "Read slowly and clearly", start: "Start reading", finish: "Finish reading", unsupported: "Hindi voice recognition is not available in this browser. Open it in Chrome or Edge.", profileTag: "BEFORE YOU VIEW YOUR SCORE", profileTitle: "Tell us about yourself", profileHelp: "Complete these short details to view your result.", name: "Full name", age: "Age", phone: "Phone number", email: "Email (optional)", place: "City / place", consent: "I agree that Rajkamal may use these details for my score.", privacy: "In this prototype, your details remain only in the current browser session and are not saved anywhere yet.", view: "View my score", result: "YOUR RESULT", great: "A beautiful reading!", score: "TOTAL SCORE", accuracy: "Accuracy", fluency: "Fluency", completion: "Completion", speed: "Speed", retry: "Read again", share: "Share result", missing: "Complete this field.", phoneError: "Enter a valid phone number.", ageError: "Enter an age from 5 to 120.", emailError: "Enter a valid email.", voiceError: "A Hindi voice is unavailable. Check your device voice settings.", copied: "Your result is copied and ready to share." },
} as const;
const empty: ReadingScore = { total: 0, accuracy: 0, fluency: 0, completion: 0, consistency: 0, wordsRead: 0, expectedWords: 0, wordsPerMinute: 0, xp: 0 };

export default function OpenReader({ passages }: Props) {
  const [language, setLanguage] = useState<Language>("hi"); const [index, setIndex] = useState(0); const [status, setStatus] = useState<Status>("ready"); const [transcript, setTranscript] = useState(""); const [interim, setInterim] = useState(""); const [seconds, setSeconds] = useState(0); const [attempts, setAttempts] = useState(0); const [score, setScore] = useState<ReadingScore>(empty); const [level, setLevel] = useState(0); const [error, setError] = useState(""); const [samplePlaying, setSamplePlaying] = useState(false); const [isSaving, setIsSaving] = useState(false); const [details, setDetails] = useState<Details>({ name: "", age: "", phone: "", email: "", place: "", consent: false }); const [errors, setErrors] = useState<Partial<Record<keyof Details, string>>>({});
  const recognition = useRef<SpeechRecognition | null>(null); const stream = useRef<MediaStream | null>(null); const audioContext = useRef<AudioContext | null>(null); const meterLastUpdated = useRef(0); const startTime = useRef(0); const resultText = useRef(""); const finalDuration = useRef(0); const keepListening = useRef(false); const frame = useRef<number | null>(null); const passage = passages[index]; const t = words[language]; const reading = status === "reading";
  useEffect(() => { if (!reading) return; const timer = window.setInterval(() => setSeconds(Math.floor((Date.now() - startTime.current) / 1000)), 1000); return () => window.clearInterval(timer); }, [reading]);
  useEffect(() => { if (!reading || !recognition.current) return; const speech = recognition.current; speech.onend = () => { if (keepListening.current) setError("Voice recognition paused. Finish this attempt or start a new one."); }; }, [reading]);
  useEffect(() => () => { stopMeter(); window.speechSynthesis?.cancel(); }, []);
  const elapsed = `${String(Math.floor(seconds / 60)).padStart(2, "0")}:${String(seconds % 60).padStart(2, "0")}`;
  function stopMeter() { if (frame.current) cancelAnimationFrame(frame.current); frame.current = null; stream.current?.getTracks().forEach((track) => track.stop()); stream.current = null; if (audioContext.current) { void audioContext.current.close(); audioContext.current = null; } setLevel(0); }
  function startMeter(input: MediaStream) { const audio = new AudioContext(); audioContext.current = audio; const analyser = audio.createAnalyser(); analyser.fftSize = 32; audio.createMediaStreamSource(input).connect(analyser); const values = new Uint8Array(analyser.frequencyBinCount); const draw = (now: number) => { analyser.getByteFrequencyData(values); if (now - meterLastUpdated.current > 100) { meterLastUpdated.current = now; setLevel(values.reduce((total, value) => total + value, 0) / values.length); } frame.current = requestAnimationFrame(draw); }; frame.current = requestAnimationFrame(draw); }
  async function startReading() { setError(""); const API = window.SpeechRecognition || window.webkitSpeechRecognition; if (!API) { setStatus("unsupported"); return; } try { const input = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true } }); stream.current = input; startMeter(input); setTranscript(""); setInterim(""); resultText.current = ""; setSeconds(0); startTime.current = Date.now(); setAttempts((value) => value + 1); keepListening.current = true; const speech = new API(); recognition.current = speech; speech.lang = "hi-IN"; speech.continuous = true; speech.interimResults = true; speech.onresult = (event) => { let final = ""; let partial = ""; for (let item = event.resultIndex; item < event.results.length; item += 1) { if (event.results[item].isFinal) final += `${event.results[item][0].transcript} `; else partial += event.results[item][0].transcript; } if (final) { resultText.current += final; setTranscript(resultText.current); } setInterim(partial); }; speech.onerror = () => setError(language === "hi" ? "Mic या voice recognition में रुकावट आई। फिर कोशिश करें।" : "Microphone or speech recognition was interrupted. Please try again."); speech.onend = () => { if (keepListening.current) try { speech.start(); } catch { /* browser restart */ } }; speech.start(); setStatus("reading"); } catch { setError(language === "hi" ? "माइक्रोफ़ोन की अनुमति दें और फिर शुरू करें।" : "Allow microphone access and try again."); } }
  function finishReading() { keepListening.current = false; recognition.current?.stop(); stopMeter(); const duration = Math.max(1, Math.floor((Date.now() - startTime.current) / 1000)); finalDuration.current = duration; setSeconds(duration); setScore(scoreReading(passage.reference_text, resultText.current, duration, attempts)); setStatus("details"); }
  function nextPassage() { setIndex((value) => (value + 1) % passages.length); setStatus("ready"); setTranscript(""); setInterim(""); setSeconds(0); setScore(empty); }
  function listen() { const synth = window.speechSynthesis; if (!synth) { setError(t.voiceError); return; } if (samplePlaying) { synth.cancel(); setSamplePlaying(false); return; } synth.cancel(); const voice = new SpeechSynthesisUtterance(passage.reference_text); voice.lang = "hi-IN"; voice.rate = .8; voice.onend = () => setSamplePlaying(false); voice.onerror = () => { setSamplePlaying(false); setError(t.voiceError); }; synth.speak(voice); setSamplePlaying(true); }
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const next: Partial<Record<keyof Details, string>> = {}; if (!details.name.trim()) next.name = t.missing; const age = Number(details.age); if (!Number.isInteger(age) || age < 5 || age > 120) next.age = t.ageError; if (details.phone.replace(/\D/g, "").length < 10) next.phone = t.phoneError; if (details.email && !/^\S+@\S+\.\S+$/.test(details.email)) next.email = t.emailError; if (!details.place.trim()) next.place = t.missing; if (!details.consent) next.consent = t.missing; setErrors(next); if (Object.keys(next).length) return; setIsSaving(true); setError(""); try { await saveReaderAttempt({ details, passage, transcript: resultText.current, durationSeconds: finalDuration.current, score }); setStatus("result"); } catch { setError(language === "hi" ? "आपके विवरण save नहीं हो पाए। Supabase setup और internet connection जाँचें।" : "We could not save your result. Check the Supabase setup and internet connection."); } finally { setIsSaving(false); } }
  async function share() { const message = language === "hi" ? `मैंने “${passage.title}” पढ़ा और ${score.total}/100 स्कोर किया।` : `I read “${passage.title}” and scored ${score.total}/100.`; if (navigator.share) await navigator.share({ title: "Rajkamal Reader", text: message }); else { await navigator.clipboard.writeText(message); setError(t.copied); } }
  const field = "mt-1.5 w-full rounded-xl border border-stone-300 bg-white px-3 py-3 font-normal outline-none focus:border-[#b42332] focus:ring-2 focus:ring-[#b42332]/15";
  return <main className="paper-grain min-h-screen pb-12"><header className="border-b border-stone-200 bg-[#fffcf7]/90 px-4 py-3 backdrop-blur sm:px-8"><div className="mx-auto flex max-w-4xl items-center justify-between gap-3"><div className="flex items-center gap-3"><Image src="/rajkamal_logo.svg" alt="Rajkamal Prakashan" width={131} height={40} priority className="h-8 w-auto max-w-32 object-contain sm:h-10 sm:max-w-44" /><div className="hidden border-l border-stone-300 pl-3 sm:block"><p className="serif text-sm font-bold">{t.title}</p><p className="text-[10px] font-bold tracking-[.14em] text-[#b42332]">{t.motto}</p></div></div><button onClick={() => setLanguage(language === "hi" ? "en" : "hi")} className="flex items-center gap-1.5 rounded-full border border-stone-300 bg-white px-3 py-2 text-xs font-bold text-[#7e1421]"><Languages className="size-3.5" />{language === "hi" ? "English" : "हिंदी"}</button></div></header><section className="mx-auto max-w-4xl px-4 pt-7 sm:px-8 sm:pt-10"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold tracking-[.18em] text-[#b42332]">{language === "hi" ? "आज का अभ्यास" : "TODAY'S PRACTICE"}</p><h1 className="serif mt-1 text-2xl font-bold sm:text-3xl">{t.motto}</h1></div><button onClick={nextPassage} disabled={reading} className="flex items-center gap-2 rounded-full border border-stone-300 bg-white px-4 py-2 text-sm font-semibold hover:border-[#b42332] hover:text-[#b42332] disabled:opacity-40"><RotateCcw className="size-4" />{t.new}</button></div><article className="overflow-hidden rounded-[1.6rem] border border-stone-200 bg-[#fffdf9] shadow-[0_16px_45px_rgba(72,44,26,.08)]"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-stone-100 bg-[#fff7ec] px-5 py-4 sm:px-8"><div><div className="mb-2 flex gap-2"><span className="rounded-full bg-[#b42332] px-2.5 py-1 text-[10px] font-bold text-white">{t.passage} {passage.sequence}/100</span><span className="rounded-full bg-white px-2.5 py-1 text-[10px] font-semibold text-stone-600">{passage.difficulty_editorial}</span></div><h2 className="serif text-xl font-bold sm:text-2xl">{passage.title}</h2></div><button onClick={listen} disabled={reading} className="flex items-center gap-2 rounded-full px-2 py-2 text-xs font-bold text-[#7e1421] disabled:opacity-40"><Volume2 className={`size-4 ${samplePlaying ? "animate-pulse text-[#b42332]" : ""}`} />{samplePlaying ? t.stop : t.listen}</button></div><div className="px-5 py-7 sm:px-8 sm:py-9"><div className="serif border-l-2 border-[#e5b043] pl-5 text-[1.3rem] leading-[2.1] text-stone-800 sm:text-[1.48rem] sm:leading-[2.2]">{passage.lines.map((line) => <p key={line}>{line}</p>)}</div><p className="mt-7 flex flex-wrap items-center gap-2 border-t border-stone-100 pt-5 text-xs text-stone-500"><BookOpen className="size-4 text-[#b42332]" />Hindi reading passage · {passage.word_count_whitespace} {language === "hi" ? "शब्द" : "words"} · {t.guide}</p></div></article><section className={`mt-5 rounded-[1.6rem] border p-5 sm:p-6 ${reading ? "border-[#b42332]/40 bg-[#7e1421] text-white" : "border-stone-200 bg-white"}`}><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className={`grid size-14 place-items-center rounded-full ${reading ? "bg-white/15" : "bg-[#fdf0ef] text-[#b42332]"}`}>{reading ? <Mic className="size-6" /> : <MicOff className="size-6" />}</div><div><p className={`text-sm font-bold ${reading ? "text-[#ffe8a7]" : "text-[#b42332]"}`}>{reading ? t.listening : t.ready}</p><p className={`mt-1 text-xs ${reading ? "text-white/70" : "text-stone-500"}`}>{reading ? t.clear : t.help}</p></div></div><div className="flex items-center justify-between gap-4 sm:justify-end"><span className="flex items-center gap-2 font-mono text-xl font-bold"><Timer className="size-4" />{elapsed}</span>{reading ? <button onClick={finishReading} className="rounded-full bg-[#e5b043] px-5 py-3 text-sm font-bold text-[#352311]">{t.finish}</button> : <button onClick={startReading} className="flex items-center gap-2 rounded-full bg-[#b42332] px-5 py-3 text-sm font-bold text-white hover:bg-[#7e1421]"><Play className="size-4 fill-current" />{t.start}</button>}</div></div>{reading && <div className="mt-5 flex h-9 items-center justify-center gap-1.5">{Array.from({ length: 22 }).map((_, item) => <span key={item} className="audio-bar w-1 rounded-full bg-[#e5b043]" style={{ height: `${20 + ((item * 23) % 65) + level / 5}%` }} />)}</div>}{(transcript || interim) && reading && <p className="mt-4 rounded-xl bg-black/10 px-4 py-3 text-sm leading-6 text-white/90">{transcript}<span className="text-[#ffe8a7]">{interim}</span></p>}{error && <p className={`mt-4 flex items-center gap-2 text-xs font-medium ${reading ? "text-[#ffe8a7]" : "text-[#b42332]"}`}><Info className="size-4" />{error}</p>}</section>{status === "unsupported" && <div className="mt-5 rounded-2xl border border-amber-300 bg-amber-50 p-5 text-sm text-amber-950">{t.unsupported}</div>}{status === "details" && <section className="mt-5 rounded-[1.6rem] border border-[#e5b043]/70 bg-[#fffaf0] p-5 shadow-sm sm:p-7"><p className="text-xs font-bold tracking-[.16em] text-[#b42332]">{t.profileTag}</p><h2 className="serif mt-1 text-2xl font-bold">{t.profileTitle}</h2><p className="mt-2 text-sm text-stone-600">{t.profileHelp}</p>{error && <p className="mt-4 flex items-center gap-2 rounded-xl bg-red-50 px-4 py-3 text-xs font-medium text-[#b42332]"><Info className="size-4" />{error}</p>}<form onSubmit={submit} className="mt-6 grid gap-4 sm:grid-cols-2"><FormField label={t.name} error={errors.name}><input value={details.name} onChange={(event) => setDetails({ ...details, name: event.target.value })} autoComplete="name" className={field} /></FormField><FormField label={t.age} error={errors.age}><input value={details.age} onChange={(event) => setDetails({ ...details, age: event.target.value })} type="number" min="5" max="120" className={field} /></FormField><FormField label={t.phone} error={errors.phone}><input value={details.phone} onChange={(event) => setDetails({ ...details, phone: event.target.value })} autoComplete="tel" inputMode="tel" placeholder="+91 98765 43210" className={field} /></FormField><FormField label={t.email} error={errors.email}><input value={details.email} onChange={(event) => setDetails({ ...details, email: event.target.value })} autoComplete="email" type="email" className={field} /></FormField><div className="sm:col-span-2"><FormField label={t.place} error={errors.place}><input value={details.place} onChange={(event) => setDetails({ ...details, place: event.target.value })} autoComplete="address-level2" className={field} /></FormField></div><label className="flex items-start gap-3 rounded-xl border border-[#eadabb] bg-white px-4 py-3 text-xs leading-5 text-stone-600 sm:col-span-2"><input checked={details.consent} onChange={(event) => setDetails({ ...details, consent: event.target.checked })} type="checkbox" className="mt-0.5 size-4 accent-[#b42332]" /><span>{t.consent}{errors.consent && <strong className="mt-1 block text-[#b42332]">{errors.consent}</strong>}</span></label><p className="text-xs leading-5 text-stone-500 sm:col-span-2"><Info className="mr-1 inline size-3.5" />{t.privacy}</p><button disabled={isSaving} className="flex items-center justify-center gap-2 rounded-full bg-[#b42332] px-5 py-3 text-sm font-bold text-white hover:bg-[#7e1421] disabled:cursor-wait disabled:opacity-60 sm:col-span-2"><Sparkles className="size-4" />{isSaving ? (language === "hi" ? "Save हो रहा है…" : "Saving…") : t.view}</button></form></section>}{status === "result" && <section className="mt-5 rounded-[1.6rem] border border-[#e5b043]/70 bg-[#fffaf0] p-5 shadow-sm sm:p-7"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div><p className="flex items-center gap-2 text-xs font-bold tracking-[.16em] text-[#b42332]"><Sparkles className="size-4" />{t.result}</p><h2 className="serif mt-2 text-3xl font-bold">{t.great}</h2></div><div className="rounded-2xl bg-[#b42332] px-6 py-4 text-center text-white"><p className="text-xs font-bold uppercase tracking-widest text-white/70">{t.score}</p><p className="serif text-4xl font-bold">{score.total}<span className="text-lg text-white/70">/100</span></p></div></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric label={t.accuracy} value={`${score.accuracy}%`} /><Metric label={t.fluency} value={`${score.fluency}%`} /><Metric label={t.completion} value={`${score.completion}%`} /><Metric label={t.speed} value={`${score.wordsPerMinute} WPM`} /></div><div className="mt-6 flex flex-col gap-3 border-t border-[#eadabb] pt-5 sm:flex-row"><button onClick={startReading} className="flex flex-1 items-center justify-center gap-2 rounded-full border border-[#b42332] px-4 py-3 text-sm font-bold text-[#b42332]"><RotateCcw className="size-4" />{t.retry}</button><button onClick={share} className="flex flex-1 items-center justify-center gap-2 rounded-full bg-[#b42332] px-4 py-3 text-sm font-bold text-white"><Share2 className="size-4" />{t.share}</button></div></section>}</section></main>;
}
function FormField({ label, error, children }: { label: string; error?: string; children: React.ReactNode }) { return <label className="text-sm font-bold text-stone-700">{label}{children}{error && <span className="mt-1 block text-xs font-medium text-[#b42332]">{error}</span>}</label>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-[#eadabb] bg-white px-3 py-4 text-center"><p className="text-[10px] font-bold tracking-wide text-stone-500">{label}</p><p className="mt-1 text-lg font-bold text-[#7e1421]">{value}</p></div>; }
